// Define the function that will be called when the button is clicked
function onClick() {
    alert("This is an awesome page");
}

// Add an event listener to the button to call the onClick function when it's clicked
document.getElementById("alertButton").addEventListener("click", onClick);